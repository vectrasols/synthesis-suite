# Optional backend interpreter override.
# SYNTHESIS_PYTHON=/absolute/path/to/python

# Optional project virtual environment directory.
# Defaults to .venv when it exists.
# SYNTHESIS_VENV=.venv

# Optional Conda environment name for contributors who prefer Conda.
# SYNTHESIS_CONDA_ENV=synthesis
